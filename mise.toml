experimental_monorepo_root = true

[env]
UV_PREVIEW_FEATURES = "add-bounds"

[monorepo]
config_roots = [
  "anki-extract-kanji",
  "anki-newlines-to-br",
  "shirabe-jisho-to-anki",
  "wanikani-apprentice",
  "wanikani-burnt-kanji-to-anki",
]

[tools]
prek = "latest"

[tasks.setup]
depends = "//...:setup"
run = [
  "prek install",
]

[tasks.upgrade-deps]
depends = "//...:upgrade-deps"
run = [
  "prek auto-update",
]

[tasks.test]
depends = "//...:test"

[tasks.lint]
run = "prek run --all-files"
