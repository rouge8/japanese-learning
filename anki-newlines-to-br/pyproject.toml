[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "anki_newlines_to_br"
authors = [{name = "Andy Freeland", email = "andy@andyfreeland.net"}]
classifiers = [
  "License :: OSI Approved :: MIT License",
  "Private :: Do Not Upload",
]
dynamic = ["version", "description"]
requires-python = "~=3.14.0"

dependencies = [
  "anki==26.5",
  "attrs==26.1.0",
  "click==8.4.2",
  "structlog==25.5.0",
]

[dependency-groups]
dev = [
    "coverage==7.13.5",
    "pyrefly==1.2.0.dev2",
    "pytest==9.1.1",
    "pytest-cov==7.1.0",
    "pytest-mock==3.15.1",
    "pytest-randomly==4.0.1",
    "pytest-unordered==0.8.0",
]

[project.scripts]
anki-newlines-to-br = "anki_newlines_to_br.__main__:main"

[project.urls]
Home = "https://github.com/rouge8/japanese-learning/tree/main/anki-newlines-to-br"

[tool.hatch.version]
path = "src/anki_newlines_to_br/__init__.py"

[tool.pytest]
minversion = "9.0"
addopts = [
  "-v",
  "-ra",
  "--strict-config",
  "--strict-markers",
]

[tool.pyrefly]
preset = "strict"

[tool.ruff.lint.isort]
known-first-party = ["anki_newlines_to_br"]
